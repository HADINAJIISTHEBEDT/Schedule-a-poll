services:
  - type: web
    name: schedule-a-poll
    runtime: docker
    plan: starter
    region: frankfurt
    rootDir: .
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
    disk:
      name: poll-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: USE_FIREBASE
        value: "true"
      - key: FIREBASE_PROJECT_ID
        value: poll-generator-f6697
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium
      - key: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
        value: "true"
      - key: HOST
        value: "0.0.0.0"
